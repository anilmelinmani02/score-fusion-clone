import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'score-fusion-clone',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
